<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mybatislearning.mapper.IndustryMapper">

    <!-- resultMap定義: カラムとフィールドのマッピング -->
    <resultMap id="industryResultMap" type="com.example.mybatislearning.entity.Industry">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- INSERT: useGeneratedKeys="true"でID自動採番 -->
    <insert id="insert" parameterType="com.example.mybatislearning.entity.Industry"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO industries (name, description, created_at, updated_at)
        VALUES (#{name}, #{description}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <!-- SELECT BY ID: パラメータバインディング -->
    <select id="selectById" parameterType="long" resultMap="industryResultMap">
        SELECT id, name, description, created_at, updated_at
        FROM industries
        WHERE id = #{id}
    </select>

    <!-- SELECT ALL -->
    <select id="selectAll" resultMap="industryResultMap">
        SELECT id, name, description, created_at, updated_at
        FROM industries
    </select>

    <!-- FIND BY NAME -->
    <select id="findByName" parameterType="string" resultMap="industryResultMap">
        SELECT id, name, description, created_at, updated_at
        FROM industries
        WHERE name = #{name}
    </select>

    <!-- UPDATE -->
    <update id="update" parameterType="com.example.mybatislearning.entity.Industry">
        UPDATE industries
        SET name = #{name},
            description = #{description},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- DELETE BY ID -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM industries
        WHERE id = #{id}
    </delete>

</mapper>
